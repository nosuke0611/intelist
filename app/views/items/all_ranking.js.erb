$('.top-area').html("<%= j render 'partial/items/items_ranking', items: @all_items %>");
$('.allranking-tab').addClass('active');
$('.monthlyranking-tab').removeClass('active');
$('.weeklyranking-tab').removeClass('active');
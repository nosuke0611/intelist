$('.top-area').html("<%= j render 'partial/items/items_list', items: @monthly_items %>");
$('.allranking-tab').removeClass('active');
$('.monthlyranking-tab').addClass('active');
$('.weeklyranking-tab').removeClass('active');
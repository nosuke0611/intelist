$('#like-icon-post-<%= @post.id.to_s %>').
  html('<%= link_to post_like_path(@post.id, @post.liked_by(current_user)), method: :delete, remote: true, class: "btn" do %><i class="fas fa-heart"></i><% end %>');